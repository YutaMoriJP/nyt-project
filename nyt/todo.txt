Search Component:
- instead of using textfield, maybe use a option bar, or have multiple tags that triggers the search

Home:
- have an info box that explains about this App

renderData fn:
- improve UI of the result box, add images (use an Image component that makes image loading efficient)
- look for examples that look stylish

improvements:
- consider writing tests
- consider adding esling
- add a11y - like image alt, <a> alt etc.
- give custamability options, like changing display, making different searches etc.
- add theme toggle
- add error handling if search keyword results in not found repsonse
- populate 10 results with fixed size for each flex item and user can click on expand that opens up a window on the same page